# PCAP-ATTACK

Container of PCAP captures mapped to the relevant attack tactic.

Recommended reviewer fixture:

```text
pcap/CredAccess/DCSync_krbtgt_dcerpc_smb.pcapng
```

The Playwright E2E test uses that fixture by default. Override with `E2E_PCAP_PATH=/path/to/file.pcapng`.
